.grid-lg
	display grid
	grid-template-columns repeat(12, 1fr)
	grid-gap $gutter $gutter

for $i in (1..12)
	.col-lg-{$i} 
		width (100 * $i / 12)%
	.gcol-lg-{$i}
		grid-column-end span $i

for $j in (1..11)
	.offset-lg-{$j}
		margin-left (100 * $j / 12)%
	.goffset-lg-{$j}
		margin-left (100 * $j / 12)%
