/**
 * base/desktop
 */

// Container
.container
	max-width $container-width-d
	&.isNarrow
		max-width $container-width-t

// Display
.desktop
	display block
	&.ib
		display inline-block
	&.inline
		display inline
	&.table
		display table
	&.table-cell
		display table-cell