{% extends "layout/base.twig" %}

{% block content %}
	<h1>{{ page.title }}</h1>
	{{ page.content }}
	{# Show posts #}
	{% for post in posts %}
		{% include 'tease.twig' %}
	{% endfor %}
{% endblock %}